import {StyleSheet} from "react-native";

export const MainTheme = StyleSheet.create({
    centerContent: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center'
    },
    text: {
        color: 'black'
    }
})

export const colors = {
    main: 'black'
}